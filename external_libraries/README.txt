Copy the 'google-play-services_lib' folder to this folder, then from this directory type this command:

android update project --path google-play-services_lib

(I think the 'subprojects' warning can be safely ignored)
